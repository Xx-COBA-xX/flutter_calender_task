import 'package:flutter/material.dart';

class AvailableDay extends StatelessWidget {
  const AvailableDay({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}